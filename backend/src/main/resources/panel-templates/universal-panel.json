{
  "type": "{{visualization}}",
  "title": "{{title}}",
  "datasource": {
    "type": "postgres",
    "uid": "{{datasource_uid}}"
  },
  "targets": [
    {
      "refId": "A",
      "datasource": {
        "type": "postgres",
        "uid": "{{datasource_uid}}"
      },
      "rawSql": "{{query}}",
      "editorMode": "code",
      "rawQuery": true
    }
  ],
  "fieldConfig": {
    "defaults": {},
    "overrides": []
  },
  "options": {}
}
