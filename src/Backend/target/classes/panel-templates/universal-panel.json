{
  "type": "{{visualization}}",
  "title": "{{title}}",
  "datasource": {
    "type": "postgres",
    "uid": "{{datasource_uid}}"
  },
  "targets": [
    {
      "refId": "A",
      "datasource": {
        "type": "postgres",
        "uid": "{{datasource_uid}}"
      },
      "rawSql": "{{query}}",
      "editorMode": "code",
      "format": "auto"
    }
  ],
  "fieldConfig": {
    "defaults": {},
    "overrides": []
  },
  "options": {}
}
